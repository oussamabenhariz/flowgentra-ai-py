name: Build & Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

env:
  OPENSSL_STATIC: "1"
  PYTHON_VERSIONS: "3.9 3.10 3.11 3.12 3.13"

# The bindings use a path dependency on the core repo
# (../flowgentra-ai-rs/flowgentra-ai), so every build job checks out both
# repos side by side — same layout as ci.yml.
jobs:
  build-linux-x86:
    name: Build wheels (Linux x86_64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: flowgentra-ai-py
      - uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/flowgentra-ai-rs
          path: flowgentra-ai-rs

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config

      - uses: actions/setup-python@v5
        with:
          python-version: |
            3.9
            3.10
            3.11
            3.12
            3.13

      - uses: dtolnay/rust-toolchain@stable

      - name: Install maturin
        run: pip install maturin

      - name: Build wheels
        working-directory: flowgentra-ai-py
        run: maturin build --release --out dist -i python3.9 -i python3.10 -i python3.11 -i python3.12 -i python3.13

      - uses: actions/upload-artifact@v4
        with:
          name: wheel-linux-x86_64
          path: flowgentra-ai-py/dist/*.whl

  build-linux-aarch64:
    name: Build wheels (Linux aarch64)
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
        with:
          path: flowgentra-ai-py
      - uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/flowgentra-ai-rs
          path: flowgentra-ai-rs

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config

      - uses: actions/setup-python@v5
        with:
          python-version: |
            3.9
            3.10
            3.11
            3.12
            3.13

      - uses: dtolnay/rust-toolchain@stable

      - name: Install maturin
        run: pip install maturin

      - name: Build wheels
        working-directory: flowgentra-ai-py
        run: maturin build --release --out dist -i python3.9 -i python3.10 -i python3.11 -i python3.12 -i python3.13

      - uses: actions/upload-artifact@v4
        with:
          name: wheel-linux-aarch64
          path: flowgentra-ai-py/dist/*.whl

  build-macos:
    name: Build wheels (macOS - ${{ matrix.target }})
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64-apple-darwin, aarch64-apple-darwin]
    steps:
      - uses: actions/checkout@v4
        with:
          path: flowgentra-ai-py
      - uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/flowgentra-ai-rs
          path: flowgentra-ai-rs

      - uses: actions/setup-python@v5
        with:
          python-version: |
            3.9
            3.10
            3.11
            3.12
            3.13

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          working-directory: flowgentra-ai-py
          target: ${{ matrix.target }}
          args: --release --out dist -i python3.9 -i python3.10 -i python3.11 -i python3.12 -i python3.13

      - uses: actions/upload-artifact@v4
        with:
          name: wheel-macos-${{ matrix.target }}
          path: flowgentra-ai-py/dist/*.whl

  build-windows:
    name: Build wheels (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: flowgentra-ai-py
      - uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/flowgentra-ai-rs
          path: flowgentra-ai-rs

      - uses: actions/setup-python@v5
        with:
          python-version: |
            3.9
            3.10
            3.11
            3.12
            3.13

      - uses: dtolnay/rust-toolchain@stable

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          working-directory: flowgentra-ai-py
          target: x64
          args: --release --out dist -i python3.9 -i python3.10 -i python3.11 -i python3.12 -i python3.13

      - uses: actions/upload-artifact@v4
        with:
          name: wheel-windows
          path: flowgentra-ai-py/dist/*.whl

  build-sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: flowgentra-ai-py
      - uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/flowgentra-ai-rs
          path: flowgentra-ai-rs

      - uses: PyO3/maturin-action@v1
        with:
          working-directory: flowgentra-ai-py
          command: sdist
          args: --out dist

      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: flowgentra-ai-py/dist/*.tar.gz

  publish:
    name: Publish to PyPI
    needs: [build-linux-x86, build-linux-aarch64, build-macos, build-windows, build-sdist]
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
